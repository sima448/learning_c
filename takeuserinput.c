#include<stdio.h>
int main() {
     int x;
     float y;
     scanf("%d", &x);//& it is the address of x and this line is used to take the input inconsole/terminal that means user input
     scanf("%f", &y);
     printf("%d\n", x);//print output
     printf("%f\n", y);
    return 0;
}
